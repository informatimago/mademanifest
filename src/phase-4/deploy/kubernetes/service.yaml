apiVersion: v1
kind: Service
metadata:
  name: mademanifest
  labels:
    app: mademanifest
spec:
  selector:
    app: mademanifest
  ports:
    - name: http
      port: 80
      targetPort: http
  type: ClusterIP
