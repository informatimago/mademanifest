apiVersion: v1
kind: ConfigMap
metadata:
  name: mademanifest-config
data:
  PORT: "8080"
  # Phase 12 retired CANON_DIRECTORY: the engine reads the canonical
  # constants directly from pkg/canon, never from a JSON file.
  SE_EPHE_PATH: /usr/local/share/swisseph
