apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - configmap.yaml
  - deployment.yaml
  - service.yaml
  # Phase 13 deployment hardening adds the two manifests below.
  - networkpolicy.yaml
  - hpa.yaml
