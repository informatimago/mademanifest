# Build artefacts produced by `make zoneinfo`.  The pinned source
# (tzdata2026a.tar.gz + SHA512SUMS + .asc) is tracked; the extracted
# IANA source tree and the compiled zoneinfo directory are not — both
# are reproducible from the tarball.
src/
zoneinfo/
