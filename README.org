#+TITLE: MadeManifest
#+OPTIONS: toc:2 num:nil

MadeManifest is a deterministic calculation engine for astrology, Human
Design, and Gene Keys.  The project is on its way from a Golden Test
proof-of-capability to the Trinity Engine v1 runtime contract described
in [[file:specifications/trinity/trinity.org][specifications/trinity/trinity.org]].

The Go engine runs as a command-line tool and as an HTTP service.  The
service is packaged for Docker and Kubernetes, and a shared test
harness exercises it in all three runtimes.

* Current Status

| Area                                    | State                                                            |
|-----------------------------------------+------------------------------------------------------------------|
| Calculation core                        | Works – UTC + Julian Day, Swiss Ephemeris 2.10.03, HD gates/lines, Gene Keys |
| Golden PoC contract (pre-Trinity)       | Green on local, docker, and kind                                 |
| Phase 0 integration harness             | Done – local + docker + kind, shared helpers                     |
| Phase 1 canon constants + version pins  | Done – =pkg/canon/constants.go=, =pkg/canon/sources.go=, =GET /version=, =--version= |
| Phase 2 Trinity input contract          | Done – =pkg/trinity/input= validator with full Document 04 rules; rejection matrix exercised through local/docker/k8s |
| Phase 3 Trinity output envelope         | Done – =SuccessEnvelope= with canon-ordered fields, =Longitude= (6 dp) + =DesignTime= (RFC 3339 UTC floor, A3) typed marshalers, placeholder success path returns HTTP 200 |
| Phase 4 Astrology calculation           | Done – =pkg/trinity/astro= computes signs, houses (A2), 13 objects (Earth = Sun + 180), and angles; Schiedam oracle frozen as the regression sentinel |
| Phase 5 HD design-time bisection solver | Done – =pkg/hd/calc.SolveDesignTime= (canon constants, pure bisection, backward-only); =pkg/trinity/hd= wraps it with the Swiss Ephemeris Sun callback; Schiedam =design_time_utc= oracle frozen at =1990-01-12T00:38:22Z=; HTTP handler emits the canonical value |
| Phase 6 HD gate/line mapping            | Done – =pkg/hd/calc.MapToGateLine= (canon constants only, anchor 277.5°, start-inclusive / end-exclusive); =pkg/trinity/hd= computes personality + design activations via SE_TRUE_NODE; =SE_NODE_POLICY= env shim removed; Schiedam personality + design activation oracle frozen |
| Phase 7 HD structural layer             | Done – =pkg/hd/structure.Compute= derives channels (lex-sorted), centers (canon order), definition (union-find over active channels), type (5-way decision tree), authority (8-way priority), profile, incarnation_cross; Schiedam structural oracle (3 channels, triple_split, projector / splenic, profile 1/3) frozen |
| Phase 8 Gene Keys canonical output      | Done – =pkg/trinity/genekeys.Compute= derives the four positions directly from HD pillar activations; canonical =life_work= field name; canon-fence JSON-key whitelist rejects any drift toward text / shadow / gift / essence fields; Schiedam Gene Keys oracle (=42.1 / 32.1 / 61.3 / 62.3=) frozen |
| Phase 9 Determinism cleanup             | Done – =pkg/canon.SelfCheck()= validates every compiled constant at boot, =ephemeris.ValidateEphePath()= sanity-checks the deployment-resolved Swiss Ephemeris path; httpserver =main()= refuses to start on either failure.  =/version= surfaces =ephe_path_resolved= (never in metadata).  =SE_NODE_POLICY= env immunity is verified end-to-end through local subprocess, Docker container, and kind cluster (kustomize overlay injects =SE_NODE_POLICY=true= and the canonical Phase 4-8 oracles must stay bit-identical). |
| Phase 10 HTTP Trinity contract          | Done – =POST /manifest= enforces =Content-Type: application/json= (415 + invalid_input on missing or wrong type), caps request bodies at =MaxRequestBodyBytes = 1 MiB= (413 + unsupported_input on overflow), and keeps the Phase 2 invalid_input classification for malformed JSON; =GET /healthz= remains liveness-only with no version leak; =AssertHTTPContract= verifies all five paths end-to-end through local / docker / kind |
| Phase 11 Golden Test Pack               | Done – =pkg/golden= loads + runs =src/golden/trinity/<category>/<name>/{input,expected}.json= with canon minimums (3 / 5 / 5 / 5 / 2 / 3) enforced at load time; success cases compare envelopes minus metadata, error cases compare =error_type= per A4; =TestTrinityGoldenPack= wired into local / docker / kind |
| Phase 12 PoC retirement                 | Done – removed =pkg/emit_golden= / =pkg/process_input= / =pkg/engine= / =pkg/human_design= / =pkg/gene_keys= / =pkg/astrology= / =pkg/geolocation=, =cmd/main.go=, =src/canon/*.json=, =src/golden/GOLDEN_TEST_CASE_V1.json=; =cmd/httpserver= is the only runtime surface; HTTP =Handler.CanonPaths= and =Processor= =canonPaths= argument retired; Makefile, Dockerfile, and dev scripts retargeted at the Trinity pack; compile-time guard rejects any future re-import of the retired packages |
| Phase 13 Deployment hardening           | Done – Dockerfile pins =golang:1.22-bookworm= and =debian:bookworm-slim= base-image digests, runs as non-root =appuser= (UID 10001), ships =HEALTHCHECK CMD curl=; deployment manifests add pod- and container-level =securityContext= (=runAsNonRoot=, =readOnlyRootFilesystem=, drop ALL caps, =seccomp=RuntimeDefault=), =/tmp= tmpfs, image digest pin; new =networkpolicy.yaml= restricts ingress to ingress-nginx; new =hpa.yaml= scales 1..10 replicas at 70% CPU; =AssertHardenedDeploymentShape= verifies posture every k8s test run; gated =AssertNetworkPolicyEnforced= and =AssertHPAScalesUnderLoad= (=TRINITY_NETWORK_POLICY_ENFORCED=1= / =TRINITY_LOAD_TEST=1=); =validate-kubernetes= CI job extended to validate the new manifests + the kustomize-assembled output |
| Canon ambiguities A1..A8                | Open – see =src/doc/version-pins.org=                            |

The next phase is Phase 14 (release + acceptance: tag
=v1.0.0-trinity= once the full pack passes on all three runtimes,
ship the acceptance report under =src/doc/trinity-acceptance-<date>.org=,
add a nightly CI job that re-runs the pack and posts the artefact).
The full plan is in
[[file:src/doc/trinity-implementation-plan.org][src/doc/trinity-implementation-plan.org]]; pinned versions and the
=A*= ambiguity register live in [[file:src/doc/version-pins.org][src/doc/version-pins.org]].

* Repository Layout

- =specifications/=
  - =Engineer_Onboarding_README_EN.pdf=, =Engineering_Implementation_Specification_EN.pdf=
  - =trinity/= – Trinity v1 canon package (Documents 00–12, =trinity.org=, =report.org=)
- =architecture/= – hybrid architecture + system flow diagrams (PDF + Mermaid)
- =src/= – everything that is built and shipped
  - =Makefile=, =Dockerfile=
  - =golden/trinity/= – Trinity Golden Test Pack (Phase 11): per-category fixture trees =<category>/<name>/{input,expected}.json=
  - =golden/trinity/baseline/= – Phase 4-8 per-section oracles for the Schiedam canonical case
  - =ephemeris/= – Swiss Ephemeris source tarball + =.se1= data files
  - =env/= – pinned Go version, Docker dev container (=env/docker/=)
  - =deploy/kubernetes/= – kustomize base for the cluster deploy
  - =scripts/= – interactive dev-loop scripts (=request_cloud_service.sh=, k8s up/down/test)
  - =doc/= – user manual, requirement tracking, Trinity implementation plan
  - =mademanifest-engine/= – Go module
    - =cmd/httpserver/= – HTTP service entry point (only runtime surface after Phase 12)
    - =pkg/= – engine packages (see below)
    - =integration/= – Trinity test harness

* Go Engine Packages

Phase 12 retired the entire PoC code path.  The packages below are
the trinity-only surface the engine ships today.

=src/mademanifest-engine/pkg/=:

- =canon= – compiled-in canonical constants (=GateOrder=, =SignOrder=, =ChannelTable=, =CenterOrder=, etc.) and version pins.  =SelfCheck()= validates them at boot; =AssertGateSequenceFileMatchesGateOrder= is a deployment / CI tool for verifying any external JSON gate-sequence fixture against the compiled canon.
- =astronomy= – local time → UTC, UTC → Julian Day.
- =ephemeris=, =sweph= – Swiss Ephemeris (cgo) wrappers, pinned to 2.10.03.  =ResolvedEphePath()= surfaces the deployment-resolved data directory for =/version=; =ValidateEphePath()= is a boot-time gate.
- =hd/calc= – Phase 5+ canonical Trinity calculation primitives.  Holds =SolveDesignTime= (pure bisection, backward-only, all canon constants compiled in) and =MapToGateLine= (mandala anchor 277.5°, start-inclusive / end-exclusive interval rule).
- =hd/structure= – Phase 7 structural derivations.  =Compute(personality, design)= produces channels (lex-sorted), centers (canon order), definition (5-class union-find), type (5-way decision tree), authority (8-way priority), profile, and incarnation_cross.
- =trinity/input= – Phase 2 Trinity input validator with full Document 04 rules.
- =trinity/output= – Phase 3 envelope types (=SuccessEnvelope=, =ErrorEnvelope=) with canon-ordered JSON keys.
- =trinity/astro= – Phase 4 Trinity astrology pipeline (signs, houses, 13 objects, angles).
- =trinity/hd= – Trinity Human Design pipeline.  =ComputeDesignTime= (Phase 5) drives the bisection solver; =ComputeActivations= (Phase 6) builds personality + design activation arrays from SE_TRUE_NODE-policy snapshots fed through the canonical mandala mapping.
- =trinity/genekeys= – Phase 8 Gene Keys derivation.  =Compute(personality, design)= reads the four HD pillar activations and emits the canonical four positions (life_work / evolution / radiance / purpose).
- =golden= – Phase 11 Golden Test Pack runner.  =LoadFixtures= walks =src/golden/trinity/<category>/<name>/{input,expected}.json=, enforces canon minimums per category, and returns deterministic =Fixture= records.  =CompareSuccess= / =CompareError= drive each fixture's response through the Trinity comparison rules (semantic JSON equality for success minus metadata; =error_type= + envelope shape for errors per A4).
- =httpservice= – =/healthz=, =/version=, and =/manifest= HTTP handlers (Phase 10 contract).

* Build And Run

** Prerequisites

- macOS or Linux
- Go 1.22 (see =src/env/GO_VERSION.txt=)
- A C toolchain for cgo
- Docker (for the container and Kubernetes harnesses)
- =kind= and =kubectl= (for the Kubernetes harness)

** Swiss Ephemeris

Build and install Swiss Ephemeris 2.10.03 and its data files (one-time
step; uses =sudo= for =rsync= into =/usr/local/share/swisseph=):

#+begin_src bash
make -C src swisseph-install swisseph-install-data
#+end_src

** Compile And Run

Phase 12 retired the file-based PoC CLI.  =cmd/httpserver= is now
the only buildable entry point.  The Makefile target =compile=
emits the binary at =src/mademanifest-engine/mademanifest-engine=:

#+begin_src bash
make -C src compile
#+end_src

Run it with sensible defaults:

#+begin_src bash
cd src/mademanifest-engine
PORT=8080 \
SE_EPHE_PATH=/usr/local/share/swisseph \
./mademanifest-engine
#+end_src

Endpoints (full status-code policy in =doc/user-manual.md=):
- =GET /healthz= – liveness probe (body =={"status":"ok"}==)
- =GET /version= – pinned canon versions + =ephe_path_resolved=
- =POST /manifest= – Trinity payload in, Trinity envelope out

Drive it with the canonical Schiedam fixture from the Trinity pack:

#+begin_src bash
src/scripts/request_cloud_service.sh \
    http://localhost:8080/manifest \
    src/out/cloud-service-output.json
#+end_src

The Makefile aliases =run=, =diff=, and =test-integration= now
delegate to =test-integration-local=, which runs the full Phase 11
golden pack against a freshly-built local subprocess.

** HTTP Service (Docker)

Build the production image:

#+begin_src bash
docker build -t mademanifest:latest src
#+end_src

Run it locally:

#+begin_src bash
docker run --rm -p 8080:8080 mademanifest:latest
#+end_src

The image installs Swiss Ephemeris at the canonical locations:

- =/usr/local/lib/libswe.so=
- =/usr/local/share/swisseph/= – =.se1= data files
- =SE_EPHE_PATH=/usr/local/share/swisseph=

Phase 12 removed the legacy =/app/canon= JSON directory: the
trinity HTTP path consumes only the compiled-in canon constants in
=pkg/canon/constants.go=, so the image no longer ships any canon
JSON files.

The Dockerfile copies the compiled library and headers directly rather
than running =make install=, because the patched Swiss Ephemeris
install target still shells out through =sudo=.

** HTTP Service (Kubernetes)

Tag and push the image to a registry your cluster can pull from (or
use =kind load docker-image= for local clusters), then apply the
kustomize base:

#+begin_src bash
kubectl apply -k src/deploy/kubernetes
kubectl rollout status deployment/mademanifest
kubectl port-forward service/mademanifest 8080:80
#+end_src

*** Interactive local-k8s dev loop (scripted)

Three helper scripts wrap the manual steps so you can spin a
throwaway cluster up, exercise it with =curl=, and tear it down.
Requires =docker=, =kind=, =kubectl= on =PATH=.

Terminal 1 – bring the service up and open a port-forward
(foreground; =Ctrl-C= deletes the cluster it created):

#+begin_src bash
src/scripts/k8s-local-up.sh
# options: --keep-cluster, --port 9090, --cluster my-cluster
#+end_src

Terminal 2 – send =GET /healthz= and =POST /manifest= through the
port-forward:

#+begin_src bash
src/scripts/k8s-local-test.sh
# overrides: URL=http://127.0.0.1:9090 FIXTURE=path/to/input.json
#+end_src

If a run crashed mid-way and left a cluster behind (or you used
=--keep-cluster= earlier), the down script removes it:

#+begin_src bash
src/scripts/k8s-local-down.sh
#+end_src

Notes:
- The up script reuses a pre-existing =trinity-dev= cluster instead
  of deleting it, so iterative runs stay fast.
- Image tag defaults to =mademanifest:dev=; the kustomize base's
  placeholder =registry.example.com/mademanifest:latest= is rewritten
  in-line before apply (no temp overlay directory).
- The test script writes the full =/manifest= response to a
  =/tmp/mademanifest-response-*.json= file and prints the path.

* Testing

** Unit Tests And Harness Self-Tests

Covers the Go packages and the =integration/= helpers' self-tests.  No
Docker or Kubernetes required.

#+begin_src bash
make -C src test
#+end_src

or, directly:

#+begin_src bash
cd src/mademanifest-engine
go test -count=1 ./pkg/... ./integration/...
#+end_src

** Phase 0 Integration Harness

Three runtime launchers share a common HTTP client (=integration/helpers.go=)
and are exercised by build-tagged smoke tests.  The same tests will
drive every Trinity fixture added in later phases.

Local subprocess (builds =cmd/httpserver= and launches it on a free
loopback port; requires the Swiss Ephemeris library and data):

#+begin_src bash
make -C src test-integration-local
#+end_src

Docker container (builds =src/Dockerfile=, runs it with the =itest=
tag, publishes container 8080 to a free host port):

#+begin_src bash
make -C src test-integration-docker
#+end_src

Kubernetes via kind (reuses a pre-existing =trinity-itest= cluster if
present, otherwise creates and tears down a fresh one; applies the
repo kustomize base through a temp overlay that pins the image tag
and opens =kubectl port-forward=):

#+begin_src bash
make -C src test-integration-k8s
#+end_src

All three:

#+begin_src bash
make -C src test-all
#+end_src

** CI

Two workflows in =.github/workflows/=:

- =ci.yml= – the original pipeline: unit tests and an HTTP smoke
  check against the PoC service.
- =trinity-ci.yml= – Phase 0 matrix: =unit=, =integration-local=,
  =integration-docker=, =integration-k8s=.  The kubernetes leg only
  runs on merges to =main= / =master= or manual dispatch.

* Trinity Specification

The canon is split across Documents 00–12 under
=specifications/trinity/=.  Two companion text files summarise the
package:

- =specifications/trinity/trinity.org= – machine-friendly canon extract
- =specifications/trinity/report.org= – canon review, ambiguities
  (A1..A7), and workstream readiness

Implementation-side documents under =src/doc/=:

- =requirement-tracking.org= – map of every Trinity requirement to the
  current code, with =Implemented= / =Partial= / =Missing= /
  =Contradicted= status
- =trinity-implementation-plan.org= – fifteen-phase plan taking the
  code from the PoC contract to the Trinity contract, with unit tests
  and local / Docker / Kubernetes integration tests at every phase
- =user-manual.md= / =user-manual.pdf= – operator manual for the
  file-based CLI

* Notes

- The project's earlier =phase-4= milestone label (a git-history
  phase, unrelated to the Trinity plan phases) has been retired: the
  HTTP service image is now tagged =mademanifest:latest= and the
  former =src/phase-4/= directory is flattened into =src/=.
- =specifications/architecture/= has moved to the repo root as
  =architecture/=.
- The former =src/README= and =src/README_RUN.md= have been folded
  into this top-level file and removed.
