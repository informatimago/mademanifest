* MadeManifest

This commit captures the phase-3 repository state: the deterministic Proof of Capability V2 bundle with its pinned canon, golden test, environment, and matching engineering specifications.

* Repository Layout

- =specifications/Engineer_Onboarding_README_EN.pdf=
- =specifications/Engineering_Implementation_Specification_EN.pdf=
- =specifications/GOLDEN_TEST_CASE_V1.pdf=
- =specifications/Proof_of_Capability_V1_ENG.pdf=
- =src/Makefile=
- =src/README=
- =src/README_RUN.md=
- =src/canon/=
- =src/golden/=
- =src/ephemeris/=
- =src/env/=
- =src/mademanifest-engine/=
- =src/tools/=

* Build And Validation

#+begin_src bash
make -C src clean compile run
make -C src clean compile diff
#+end_src

The engine reads the canonical rules from =src/canon/= and validates against =src/golden/GOLDEN_TEST_CASE_V1.json=.

* Notes

- The specification PDFs live under =specifications/= with stable filenames across the successive repository snapshots.
- The previous phase-2 layout has been replaced by the self-contained PoC v2 bundle.
